'use strict';

import {describe, before, after, it} from 'mocha';
import * as chai from 'chai';

const expect = chai.expect;

describe('Robbyson Abstract Framework Tests', () => {

    before(() => {
        return false;
    });

    after(() => {
        return true;
    });

    describe('Test...', () => {
        it('should...', () => {
            expect('TO DO');
        });
    });

});
